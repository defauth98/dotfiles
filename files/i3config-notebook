#  https://i3wm.org/docs/userguide.html for a complete reference/
set $mod Mod4

font pango:DejaVu Sans Mono 8

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Sound config
exec --no-startup-id pulseaudio
exec --no-startup-id pa-applet
bindsym $mod+Ctrl+m exec pavucontrol

# start a terminal
bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+Shift+q kill

# launch rofi 
bindsym $mod+p exec "rofi -combi-modi window,drun -theme Arc-Dark -font 'hack 14' -show combi -icon-theme 'Flat-Remix' -show-icons"
bindsym $mod+Tab exec "rofi -combi-modi window,drun -theme Arc-Dark -font 'hack 14' -show window -icon-theme 'Flat-Remix' -show-icons"

# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ccedilla focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ccedilla move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+Alt+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+t layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+n focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1 1:   BROWSER"
set $ws2 "2 2:   TERMINAL"
set $ws3 "3 3:   CODE"
set $ws4 "4 4:   TOOLS"
set $ws5 "5 5:   DOCS"
set $ws6 "6 6:   OTHERS"
set $ws7 "7 A:   FILES"
set $ws8 "8 S:   BROWSER"
set $ws9 "9 D:   MUSIC+TIMER"
set $ws10 "10 F:   OTHERS"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6

bindsym $mod+a workspace number $ws7
bindsym $mod+s workspace number $ws8
bindsym $mod+d workspace number $ws9
bindsym $mod+f workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6

bindsym $mod+Shift+a move container to workspace number $ws7
bindsym $mod+Shift+s move container to workspace number $ws8
bindsym $mod+Shift+d move container to workspace number $ws9
bindsym $mod+Shift+f move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+u restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+i exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ccedilla resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+bracketleft  workspace prev
bindsym $mod+bracketright workspace next

# Open most used programs
bindsym $mod+Mod1+f exec "thunar"
bindsym $mod+Mod1+g exec "gimp"
bindsym $mod+Mod1+b exec "brave"
bindsym $mod+Mod1+n exec "nitrogen"
bindsym $mod+Mod1+w exec "libreoffice"

#gaps
smart_gaps on
smart_borders on
gaps inner  2
gaps outer  2
gaps horizontal 1
gaps vertical 1

# workspace $ws2 gaps [inner|outer|horizontal|vertical|top|left|bottom|right] <px>
for_window [class="^.*"] border pixel 3
new_window 1pixel

## Theme colors
## class                 border  backgr. text    indic.  child_border
client.focused           #1e0331 #1e0331 #ffffff #1e0331
client.focused_inactive  #000000 #000000 #ffffff #000000  
client.unfocused         #000000 #000000 #ffffff #000000 
client.urgent            #db0d97 #db0d97 #ffffff #db0d97

# Workspace
workspace 1 output HDMI1
workspace 2 output HDMI1
workspace 3 output HDMI1
workspace 4 output HDMI1
workspace 5 output HDMI1
workspace 6 output HDMI1
workspace 7 output eDP1
workspace 8 output eDP1
workspace 9 output eDP1
workspace 10 output eDP1

bar {
  i3bar_command i3bar
  status_command i3status -c /home/defauth/.config/i3status/i3status.conf

  mode dock

	bindsym button4 nop
	bindsym button5 nop
	font xft:Noto Sans 7
	strip_workspace_numbers yes

    colors {
        background #000000
        statusline #ffffff
        separator #666666

        focused_workspace  #777070 #777070 #ffffff
        active_workspace   #333333 #5f676a #ffffff
        inactive_workspace #333333 #222222 #888888
        urgent_workspace   #2f343a #900000 #ffffff
        binding_mode       #2f343a #900000 #ffffff
    }
}

# Auto move windows

# Workspace 1 - navegaodr
for_window [class="Brave-browser"] move to workspace $ws1 focus

# Workspace 2 - suite office e os terminais
# veja uma suite office do seu ag'rado para usar tanto no windows quanto no linux
#for_window [class="DesktopEditors"] move to workspace $ws2 focus

# Workspace 3 - IDE de desenvolvimento
for_window [class="Code"] move to workspace $ws3 focus
for_window [class="Electron"] move to workspace $ws3 focus
for_window [class="jetbrains-studio"] move to workspace $ws3 focus
for_window [class="jetbrains-idea-ce"] move to workspace $ws3 focus

# Workspace 4 - tools
for_window [class="Insomnia"] move to workspace $ws4 focus
for_window [class="Postbird"] move to workspace $ws4 focus
for_window [class="Gimp"] move to workspace $ws4 focus
for_window [class="Nitrogen"] move to workspace $ws4 focus

# Workspace 5 - Docs
for_window [class="DesktopEditors"] move to workspace $ws5 focus
for_window [class="^libreoffice"] move to workspace $ws5 focus

# Workspace 6 - OTHERS
for_window [class="discord"] move to workspace $ws6 focus
for_window [class="zoom"] move to workspace $ws6 focus
for_window [class="Pamac-manager"] move to workspace $ws6 focus

# Workspace 7:A - files 
for_window [class="Org.gnome.Nautilus"] move to workspace $ws7 focus
for_window [class="Thunar"] move to workspace $ws7 focus

# Workspace 8:S - browser
#for_window [class="firefox"] move to workspace $ws8 focus

# Workspace 9:D - music+timer
for_window [class="productivity-timer"] move to workspace $ws9 focus
for_window [class="vlc"] move to workspace $ws9 focus
for_window [class="banshee"] move to workspace $ws9 focus

# Workspace 10:F - vlc, media players and spotify
#for_window [class="x"] move to workspace $ws10 focus

# Another windows configs
for_window [class="GParted"] floating enable 
for_window [class="^Gnome-calculator"] floating enable 
for_window [class="banshee"] floating enable

# Startup programs
exec_always --no-startup-id xrandr --output eDP1 --auto --left-of HDMI1
exec --no-startup-id nitrogen --restore; sleep 1; picom
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnoome-authentication-agent-1
exec --no-startup-id xfce4-power-manager
exec --no-startup-id pamac-tray
exec --no-startup-id clipit
exec_always --no-startup-id ff-theme-util
exec_always --no-startup-id fix_xcursor

